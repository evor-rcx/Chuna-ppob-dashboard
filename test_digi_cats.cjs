const axios = require('axios');
async function test() {
    try {
        const username = "samsulsifa88rV8G";
        const apiKey = "dev-4f8be225-b6ff-4bd8-ac0d-58b2923e3e03"; // we need the actual dev or prod API key
        console.log("No credentials, let's just use getDigiflazzProducts from server.ts");
    } catch(e) {}
}
test();
