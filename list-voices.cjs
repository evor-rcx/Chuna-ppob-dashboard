const { EdgeTTS } = require('node-edge-tts');

async function test() {
    console.log("Checking voices...");
    // node-edge-tts doesn't have a listVoices method built-in easily, but let's see.
}
test();
